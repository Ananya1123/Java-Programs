import java.util.*;

public class INPUT{
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String name = sc.next();
// next()--reads only read only the next token from input, not the full line.
//Use nextLine()--If you want to read the whole line
        System.out.println(name);
    }
}
